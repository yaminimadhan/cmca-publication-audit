# UI – Upload & Create Project

Run with:
    streamlit run app.py


Included:
- app.py → contains two buttons:
    📤 Upload New PDF → opens upload form
    ➕ Create New Project → opens project form
- static/css/tokens.css → styles for buttons (black, white text, bold, hover)
- Dependencies listed in requirements.txt → install first with: 
	pip install -r requirements.txt


Notes:
- Forms open inline (not new pages).
- Submit/Cancel closes the form.
- TODO placeholders added for backend API calls.
- Install dependencies first:
    pip install -r requirements.txt
